import React from 'react';
import { UnderConstruction } from '../UnderConstruction';
interface ReportPageProps {
  title: string;
}
export function ReportPage({ title }: ReportPageProps) {
  return <UnderConstruction />;
}