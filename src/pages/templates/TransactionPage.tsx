import React from 'react';
import { UnderConstruction } from '../UnderConstruction';
interface TransactionPageProps {
  title: string;
}
export function TransactionPage({ title }: TransactionPageProps) {
  return <UnderConstruction />;
}