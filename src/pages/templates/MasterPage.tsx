import React from 'react';
import { UnderConstruction } from '../UnderConstruction';
interface MasterPageProps {
  title: string;
}
export function MasterPage({ title }: MasterPageProps) {
  return <UnderConstruction />;
}