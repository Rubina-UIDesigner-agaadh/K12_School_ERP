import React from 'react';
import { UnderConstruction } from '../UnderConstruction';
interface DashboardPageProps {
  title: string;
}
export function DashboardPage({ title }: DashboardPageProps) {
  return <UnderConstruction />;
}