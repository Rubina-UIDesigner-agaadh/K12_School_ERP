import React from 'react';
import { UnderConstruction } from '../UnderConstruction';
interface ListPageProps {
  title: string;
}
export function ListPage({ title }: ListPageProps) {
  return <UnderConstruction />;
}